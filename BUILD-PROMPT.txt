Build me a personal expense tracker web app called Ledger, in a new folder expense-tracker-live/. Use Next.js (App Router, TypeScript, Tailwind v4). It should have a clean dashboard: total spent this month vs. a monthly budget, a "spending by category" chart, and a recent-transactions list. Seed it with about 20 realistic sample expenses in an editable lib/ module.

Most important: include yourself in it. Add a CopilotKit chat sidebar that connects to your own Hermes AG-UI adapter, so I can open the app and ask you, right there in the sidebar, to change how it looks and works — and see the code update live. Follow the copilotkit-hermes-app skill for the exact AG-UI wiring (versions, the @ag-ui/hermes pkg.pr.new dependency, the /api/copilotkit route, the CopilotSidebar provider). When you're done, run a TypeScript typecheck and tell me how to run it.
